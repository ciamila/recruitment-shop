{% extends 'base.html.twig' %}

{% block body %}
    Hello in My Store!

    <a href="{{ path('product_listing') }}">List products</a>
    <a href="{{ path('product_add') }}">Add product</a>
{% endblock %}
